<%= form_with(model: proveedor, local: true) do |form| %>
  <% if proveedor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proveedor.errors.count, "error") %> prohibited this proveedor from being saved:</h2>

      <ul>
      <% proveedor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :proveedor_nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion, id: :proveedor_descripcion %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.email_field :email, id: :proveedor_email %>
  </div>

  <div class="field">
    <%= form.label :telefono %>
    <%= form.phone_field :telefono, id: :proveedor_telefono %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
