<%= form_with(model: insumo, local: true) do |form| %>
  <% if insumo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(insumo.errors.count, "error") %> prohibited this insumo from being saved:</h2>

      <ul>
      <% insumo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_area :nombre, id: :insumo_nombre %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_area :tipo, id: :insumo_tipo %>
  </div>

  <div class="field">
    <%= form.label :presentacion %>
    <%= form.text_area :presentacion, id: :insumo_presentacion %>
  </div>

  <div class="field">
    <%= form.label :precio %>
    <%= form.text_field :precio, id: :insumo_precio %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
