<%= form_with(model: con_in, local: true) do |form| %>
  <% if con_in.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(con_in.errors.count, "error") %> prohibited this con_in from being saved:</h2>

      <ul>
      <% con_in.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :con_in_nombre %>
  </div>

  <div class="field">
    <%= form.label :consulta_id %>
    <%= form.text_field :consulta_id, id: :con_in_consulta_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
